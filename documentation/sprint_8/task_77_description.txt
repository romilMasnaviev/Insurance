TODO**Контекст задачи (краткое описание) **
Ментор обратил ваше внимание на проблему возникшую при
написании тестов на контроллер. Суть проблемы в следующем.
Для сравнения JSON строк на данный момент используется
следующий код из класса TravelCalculatePremiumControllerTest:

ObjectMapper mapper = new ObjectMapper();
assertEquals(mapper.readTree(responseBodyContent), mapper.readTree(jsonResponse));

Проблема в том, что если сравниваемые JSON строки отличаются
друг от друга только перестановкой элементов, содержат одни
и те же элементы только в разном порядке то код выше считает
такие JSON строки разными.

TODO**Что было сделано и как? (краткое описание)**
Установил библиотеку
uk.org.webcompere:model-assert:1.0.0.
Переделал метод для сравнения Json объектов
assertJson(expectedResponse)
                .where()
                .keysInAnyOrder()
                .arrayInAnyOrder()
                .isEqualTo(actualResponse);
Дополнительные настройки - ключи в любом порядке,
а также значения внутри массива в любом порядке.

TODO**Зачем это было сделано? Чему научился в процессе выполнения задания?**
Для того, чтобы отдалиться от строк и больше работать как с объектами. Это позволяет
более гибко писать тесты.

TODO**Сколько потратил времени на выполнение задачи?**
20 минут

TODO**Заметки, идеи, предложения в свободной форме**