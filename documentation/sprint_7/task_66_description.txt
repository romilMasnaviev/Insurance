TODO**Контекст задачи (краткое описание) **
Ментор предложил добавить функционал сохранения запросов
и ответов (JSON) в отдельный лог файл (не со всеми логами
как это сделано сейчас). Это может быть полезно для их
дальнейшего анализа.

Ваша задача: добавить конфигурацию еще одного file appender
в logback.xml (с названием файла request-response-logger.log),
а так же конфигурацию для логирования запроса и ответа в новый аппендер.

TODO**Что было сделано и как? (краткое описание)**
Добавил новый аппендер и два новых логера в logback.xml. Т.к. в logback есть наследование,
и логеры используют аппендеры, указанные в корневом логере, пришлось явно создать два логера,
отключить у них наследование и указать необходимый аппендер.

TODO**Зачем это было сделано? Чему научился в процессе выполнения задания?**
Чтобы логи различных слоев/различных потребностей писались в разные файлы. Это очень логично

TODO**Сколько потратил времени на выполнение задачи?**
30 минут

TODO**Заметки, идеи, предложения в свободной форме**